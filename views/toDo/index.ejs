<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <a href='/toDo/add' class='btn btn-primary btn-block'>New Item</a>
            <br />
             <table class='table table-hover table-striped table-bordered'>
                <tr>
                    <th>Title</th>
                    <th>Category Name</th>
                    <th>Due Date</th>
                    <th>Mark as Completed</th>
                </tr>
                <% toDos.forEach(function(toDo) { %>
                    <tr  class="<%= toDo.DueDate < new Date() ? 'table-danger' : '' %>"      >
                        <td><%= toDo.Title %></td>
                        <td><%= toDo.Name %></td>
                        <td><%= toDo.DueDate.toDateString() %></td>
                        <td>
                            <form method="post" action="/toDo/markascompleted">
                                <input name="id" value="<%= toDo.Id %>" type="hidden" />
                                <button class="btn btn-primary">Mark as Completed</button>
                            </form>
                        </td>
                    </tr>
                  <% }); %>
            </table>
        </div>
    </div>
</div>